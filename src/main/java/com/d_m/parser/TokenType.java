package com.d_m.parser;

public enum TokenType {
    LEFT_PAREN, // '('
    RIGHT_PAREN, // ')'
    COLON, // ':'
    COMMA, // ','
    ASSIGN, // ':='
    SEMICOLON, // ';'
    IDENTIFIER,
    NUMBER,
    BEGIN,
    END,
    VAR,
    PROCEDURE,
    FUNCTION,
    INTEGER,
    BOOLEAN,
    IF,
    THEN,
    ELSE,
    WHILE,
    DO,
    AND,
    OR,
    NOT,
    TRUE,
    FALSE,
    VOID,

    EOF
}
